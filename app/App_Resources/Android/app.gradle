// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.yunity.bodythermometer"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
